# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log

# apnaGit VCS — local repo directories created by CLI (never commit these)
.apnaGit/

# Temporary commit staging file
commit.json

# AWS credentials (use env vars or AWS CLI profile instead)
.aws/
aws-credentials*

# OS files
.DS_Store
